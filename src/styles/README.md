# styles/

Global styles live in `src/index.css` (Tailwind entry + design tokens),
per Vite convention. This folder is reserved for any future modular
stylesheets (e.g. print styles for the resume route, or third-party
overrides) that shouldn't live in components.
