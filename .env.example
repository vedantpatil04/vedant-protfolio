# Backend API base URL for production (e.g. deployed on Render).
# In production (e.g. Vercel dashboard environment variables):
# VITE_API_BASE_URL=https://vedant-protfolio.onrender.com
#
# In local development: leave blank or omit to use the Vite proxy (/api -> http://localhost:4000/api)
VITE_API_BASE_URL=https://vedant-protfolio.onrender.com
